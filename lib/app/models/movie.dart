class Movie {
  int id;
  String name;
  String category;
  String image;
  String extraInfo;
  bool isSelected;

  Movie({this.id, this.name, this.category, this.extraInfo, this.isSelected = false, this.image});
}
